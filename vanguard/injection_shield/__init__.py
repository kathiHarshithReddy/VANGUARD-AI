"""
Injection Shield package — AST-based SQL / NoSQL / GraphQL validation.

Modules:
  ast_parser      — parse query strings into abstract syntax trees
  grammar_learner — learn expected grammar patterns from legitimate queries
  validator       — contextual injection detection using AST + learned grammar
"""
